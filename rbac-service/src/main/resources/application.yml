server:
  port: 8081

spring:
  application:
    name: RBAC-SERVICE

  datasource:
    url: jdbc:postgresql://localhost:5432/rbac_db
    username: postgres
    password: naveen

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka